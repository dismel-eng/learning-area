'use strict';
let title = 'Seed Project';
let heading = document.createElement('h1');
heading.textContent = title;

let main_content = document.getElementById('main');

main_content.appendChild(heading);