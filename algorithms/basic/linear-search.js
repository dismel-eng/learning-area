/** LINEAR SEARCH
 * Input: A sequence of n numbers A = (a1, a2, . . . .an) and a value v.
 * Output: An index i such that v = A[i] or the special value NIL if v does not appear in A.
 *
 * 
 *  Seudo Code: 
 *  Parameter: an array A = [5,2,4,6,1,3] v = 1 
 *
 *  for i = 0; i < A.length; i++
 *      if A[i] == v            
 *          return i
 *  return NIL
 */